import XCTest
@testable import early_stoping

final class early_stopingTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(early_stoping().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
